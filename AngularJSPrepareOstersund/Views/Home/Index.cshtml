<div class="row" ng-app="testApp">

    <div ng-controller="testController">
        

        <div class="jumbotron" ng-init="getData()">
            <h2>Learning Angular with webAPI</h2>
            <p ng-repeat="person in data track by $index">Namn: {{person.Name}} Ålder: {{person.Age}}</p>
        </div>
        <input type="text" ng-model="person.Name" placeholder="enter name" />
        <input type="text" ng-model="person.Age" placeholder="enter age" />
        <button ng-click="sendData()">Send Data</button>

        
        
    </div>

    {{5+5}}
</div>
